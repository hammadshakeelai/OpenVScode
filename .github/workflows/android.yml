name: Build Android APK

on:
  push:
    branches: [master, main]
    tags: ['v*']
  pull_request:
    branches: [master, main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Assemble debug APK
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install SDK platform and build tools
        run: sdkmanager --install "platforms;android-35" "build-tools;35.0.0"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Assemble debug APK
        working-directory: android
        run: gradle --no-daemon assembleDebug

      - name: Stage APK with a stable name
        run: |
          set -e
          src="android/app/build/outputs/apk/debug/app-debug.apk"
          test -s "$src"
          mkdir -p dist
          cp "$src" dist/OpenVScode-Mobile.apk
          ls -lh dist/OpenVScode-Mobile.apk

      - name: Upload APK as a build artifact
        uses: actions/upload-artifact@v4
        with:
          name: OpenVScode-Mobile-apk
          path: dist/OpenVScode-Mobile.apk
          if-no-files-found: error

      - name: Publish APK to the GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: dist/OpenVScode-Mobile.apk
          generate_release_notes: true
